import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const MyTrip = (props: Props) => {
  return (
    <View>
      <Text>MyTrip</Text>
    </View>
  );
};

export default MyTrip;

const styles = StyleSheet.create({});
